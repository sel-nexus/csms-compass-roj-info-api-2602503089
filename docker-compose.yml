services:
  backend:
    build: ./backend
    environment:
      PORT: 8000
      CLIENT_API_KEYS: dev-operator-key
    ports: ["8000:8000"]
  frontend:
    build: ./frontend
    ports: ["8080:80"]
    depends_on: [backend]
