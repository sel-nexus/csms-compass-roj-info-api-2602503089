import { render,screen } from '@testing-library/react'; import {expect,it} from 'vitest'; import {DetailsView} from '../src/views/DetailsView'; import {StatusView} from '../src/views/StatusView'; it('renders safe details and status',()=>{render(<DetailsView journey={{VIN:'V',dealer:'D'}} onNew={()=>{}}/>);expect(screen.getByText('D')).toBeInTheDocument();render(<StatusView message="No journey found; review the lookup." onNew={()=>{}}/>);expect(screen.getByText(/No journey/)).toBeInTheDocument();});